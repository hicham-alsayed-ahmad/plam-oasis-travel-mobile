import 'package:flutter/material.dart';

class Style {
  static const Color primaryColor = Color(0xff005d84);
  static const TextStyle titleText =
      TextStyle(fontSize: 20, color: Colors.grey);
  static const TextStyle bodyText = TextStyle(fontSize: 16, color: Colors.grey);

  static const Padding primaryPadding = Padding(padding: EdgeInsets.all(8));
}
